# Personal tokens for authenticated git URLs (GitHub + dev.azure.com/intappdevops)
# Copy to ~/.config/personal-tokens.env and fill real values. Never commit that file.
# Owner/org is taken from the input path; URL is https://owner:TOKEN@...
#
# GitHub: use one default and/or per-org overrides (script picks by repo owner).
#   GITHUB_TOKEN = fallback for any org
#   GITHUB_TOKEN_DeepSpringAI = repos under github.com/DeepSpringAI
#   GITHUB_TOKEN_foundation_models = repos under github.com/foundation-models

# GitHub — default (optional if you set per-org below)
export GITHUB_TOKEN="YOUR_GITHUB_PERSONAL_ACCESS_TOKEN"

# GitHub — per-org (optional; overrides GITHUB_TOKEN for that org)
# export GITHUB_TOKEN_DeepSpringAI="YOUR_DEEPSPRING_GITHUB_TOKEN"
# export GITHUB_TOKEN_foundation_models="YOUR_FOUNDATION_MODELS_GITHUB_TOKEN"

# Azure DevOps — dev.azure.com/intappdevops (org from path)
export AZURE_DEVOPS_TOKEN="YOUR_AZURE_DEVOPS_PAT"

# Artifactory Docker — docker.artifactory.dev.intapp.com (for solver-sandbox make docker-push)
# export ARTIFACTORY_DOCKER_USER="your_artifactory_username"
# export ARTIFACTORY_DOCKER_TOKEN="YOUR_ARTIFACTORY_DOCKER_TOKEN"
